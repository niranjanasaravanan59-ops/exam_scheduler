# AI Audit Log — Examination Scheduler & Results Publisher

| # | Task | AI Used | Accepted | Modified | Rejected | Why |
|---|------|---------|----------|----------|----------|-----|
| 1 | Scaffold Express project structure with modular routing (auth, exam, result, dashboard, metrics) | Claude (claude.ai) | Accepted folder layout, route naming, middleware wiring | Changed `app.js` → `index.js`; moved DB connect into `config/db.js` rather than inline in `index.js` | Rejected AI's suggestion to keep all routes in a single `routes/index.js` barrel file | Barrel file obscures which module owns which route; modular files make ownership explicit and easier to test in isolation |
| 2 | Design the Result model with server-side grade computation | Claude (claude.ai) | Accepted `beforeValidate` hook for grade, `version` field for OCC, `importBatch` for idempotency | Changed grade enum from string literals to Sequelize ENUM type; added `publishedBy` and `publishedAt` fields | Rejected AI suggestion to add a `grade` column default of `'F'` at DB level | Default at DB level allows a `NULL` marks row to silently grade as `F`; the hook must always run, so a DB default is misleading |
| 3 | Implement clash detection SQL query | Claude (claude.ai) | Accepted the half-open interval condition: `startTime < newEnd AND endTime > newStart` | Verified boundary case manually (exam A ends 11:00, exam B starts 11:00 → should NOT clash); confirmed AI logic was correct but added explicit unit tests to document the boundary | Rejected AI's initial suggestion to use `Op.between` for time overlap | `BETWEEN` is inclusive on both ends and would incorrectly flag back-to-back exams as clashing |
| 4 | Generate seed data for 50+ students, faculty, exams, and results for bulk import CSV | Claude (claude.ai) | Accepted the overall structure of `seed.js` and the `sample_results.csv` template | Added `importBatch` UUID in the seed so idempotency can be tested; manually verified FK integrity (examId/studentId must exist before importing) | Rejected AI-generated password hashes (AI used a static bcrypt round-count of 1 for "speed") | Round-count 1 is insecure even in seeds because seeds are sometimes copied to staging; used 10 rounds |
| 5 | Write optimistic concurrency conflict handler in `resultController.js` | Claude (claude.ai) | Accepted the `version` mismatch check pattern and the `currentData` payload returned in the 409 body | Added `options: ['RELOAD_LATEST', 'RETRY_CHANGES', 'COMPARE_VERSIONS']` to response body (AI omitted this but the assignment spec required it) | Rejected AI suggestion to auto-increment version in the DB trigger instead of in the UPDATE query | Trigger-based increment hides the concurrency logic from the application layer; the `WHERE id = ? AND version = ?` pattern makes the intent explicit |
| 6 | Implement CSV bulk import with idempotency | Claude (claude.ai) | Accepted batch-of-50 processing loop, `uuidv4()` batchId, `findOrCreate` pattern for idempotency | Changed AI's `upsert` approach to `findOne` + conditional `create` so we can count "skipped" vs "imported" accurately | Rejected AI suggestion to use the CSV filename as the idempotency key | Filename is not stable (users rename files); a content-derived batchId stored in the DB column is the correct anchor |
| 7 | Design the result publishing workflow (Draft → Ready → Published) | Claude (claude.ai) | Accepted the `WORKFLOW_STATES` constant object and the transition validation pattern | Added `published` → reject all further transitions (AI allowed re-publishing); added `publishedAt` timestamp on transition | Rejected AI suggestion to store workflow state in a separate `result_workflows` table | Over-engineered for this scale; a `status` column on the `results` table with server-enforced transitions is sufficient |
| 8 | Write Playwright UI test for faculty tripwire (marks before exam date) | Claude (claude.ai) | Accepted the login + navigate + form-submit + assertion structure | Replaced hardcoded `page.waitForTimeout(3000)` with `page.waitForSelector('.tripwire-alert')` | Rejected AI-generated `beforeAll` that created users directly via Sequelize inside the test file | Playwright tests must not import server-side ORM models; they should call the HTTP API or use seeded DB state |
| 9 | Build the `/metrics` endpoint (operation counts, avg latency, p95 latency) | Claude (claude.ai) | Accepted in-memory metrics store design with `record()` helper and p95 calculation | Added reset-on-startup warning to README since the store is volatile | Rejected AI suggestion to use `prom-client` (Prometheus library) | `prom-client` requires a Prometheus scraper to be useful; for this assignment a plain JSON `/metrics` endpoint is more self-contained |
| 10 | Implement local draft recovery (survive page refresh) | Claude (claude.ai) | Accepted the `useDraftRecovery` hook structure with localStorage key scoped to `examId` | Added timestamp to stored draft so stale drafts (>24h) are auto-cleared | Rejected AI suggestion to use `sessionStorage` | `sessionStorage` does not survive a page refresh in some browser configs; `localStorage` with explicit expiry is the correct choice |
